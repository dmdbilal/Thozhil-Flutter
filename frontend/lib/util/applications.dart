class Applications {
  var applied;
  var pending;
  var rejected;
  var accepted;

  Applications({
    required this.applied,
    required this.pending,
    required this.rejected,
    required this.accepted,
  });
}